{
  "name": "ngo-ssi-2",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run start:dev --workspace=backend",
    "dev": "npx concurrently -n \"frontend,backend\" -c \"blue,green\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "db:up": "docker-compose up -d",
    "db:setup": "docker-compose up -d && sleep 3 && npm run prisma:setup --workspace=backend",
    "prisma:setup": "npx prisma db push && npx prisma generate && npm run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
